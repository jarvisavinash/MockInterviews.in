@{
    ViewData["Title"] = "Dashboard";
}

<h2>Welcome to your Dashboard</h2>

<div>
    <p>Hello, @User.Identity.Name! You are logged in as an @User.Claims.FirstOrDefault(c => c.Type == "Role")?.Value.</p>
</div>

<!-- Add more content or features here as needed -->
<div>
    <h3>Your Information</h3>
    <p>Email: @User.Identity.Name</p>
    <p>Role: @User.Claims.FirstOrDefault(c => c.Type == "Role")?.Value</p>
</div>

<div>
    <h3>Actions</h3>
    <ul>
        <li><a href="/Account/Logout">Logout</a></li>
        <!-- Add links to other pages, features, etc. -->
    </ul>
</div>
